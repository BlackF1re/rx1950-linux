--- a/arch/arm/mach-s3c/mach-rx1950.c
+++ b/arch/arm/mach-s3c/mach-rx1950.c
@@ -287 +287 @@
-		.default_trigger	= "main-battery-full",
+		.default_trigger	= "heartbeat",
@@ -754,0 +755,4 @@ static void __init rx1950_map_io(void)
+	/* Boot probe stage 1: the S3C24xx register map is live. */
+	gpio_direction_output(S3C2410_GPA(6), 1);
+	gpio_direction_output(S3C2410_GPA(7), 0);
+
@@ -768,0 +773,4 @@ static void __init rx1950_init_time(void)
+	/* Boot probe stage 2: clocks and the timer are configured. */
+	gpio_direction_output(S3C2410_GPA(6), 0);
+	gpio_direction_output(S3C2410_GPA(7), 1);
+
