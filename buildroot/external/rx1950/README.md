# Buildroot external tree

This directory contains the rx1950-specific Buildroot integration layer.

Planned components:

- board configuration
- root filesystem overlay
- custom packages
- image generation hooks
- reproducible build metadata
