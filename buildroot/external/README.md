# Buildroot external tree

This directory contains the rx1950-specific Buildroot extensions.

Planned components:

- board configuration
- packages
- root filesystem overlay
- image generation helpers

The build is pinned to known upstream versions to keep generated images reproducible.
